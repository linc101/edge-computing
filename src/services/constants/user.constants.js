import { user, namespace } from '../../api.json';

export const namespaceListApi = `${user}/list`;

export const namespaceDetailApi = `${namespace}/detail/{namespace}`;

export const userLoginApi = `${user}/login`;

export const userInfoApi = `${user}/user`;
